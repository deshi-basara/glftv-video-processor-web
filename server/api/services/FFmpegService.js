var ffmpeg = require('fluent-ffmpeg');

/**
 * FFmpegService.js
 *
 * FFmpeg service for converting uploaded files.
 */
module.exports = {

    convert: function(filename, inputPath, outputPath, profile) {

    }
};